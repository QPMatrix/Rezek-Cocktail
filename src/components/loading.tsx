const Loading = () => {
  return (
    <main className="min-h-screen flex items-center justify-center ">
      <img src="/loading.svg" alt="loading" className="size-48" />
    </main>
  );
};

export default Loading;
